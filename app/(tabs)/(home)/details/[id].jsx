import React, { useState } from "react";
import { SafeAreaView } from "react-native-safe-area-context";
import {
  FlatList,
  Image,
  RefreshControl,
  Text,
  View,
  Dimensions,
  TextInput,
  ScrollView,
  TouchableOpacity
} from "react-native";

const Details = () => {
    return (
        <SafeAreaView>
        <Text>Details</Text>
        </SafeAreaView>
    );
    }